# requirements.txt

# Dependências Core do Django e Servidor de Produção
Django~=5.0
gunicorn~=22.0.0
whitenoise~=6.6.0
django-cors-headers~=4.3.1

# Conexão com o Banco de Dados (Supabase - PostgreSQL)
psycopg2-binary~=2.9.9
dj-database-url~=2.1.0

# Gerenciamento de Variáveis de Ambiente (para desenvolvimento local)
python-dotenv~=1.0.1

# Integração com Armazenamento Cloud (Cloudflare R2 / S3 API)
# Necessário para gerenciar arquivos estáticos e de mídia em produção.
django-storages~=1.14.2
boto3~=1.34.113

# Pacotes sugeridos para aprimoramento da área Admin/Backend:
# django-filter~=2.5.0  # (Opcional: Se precisar de filtragem avançada de Leads)
# django-environ~=0.11.2 # (Opcional: Alternativa robusta para gerenciar settings)
